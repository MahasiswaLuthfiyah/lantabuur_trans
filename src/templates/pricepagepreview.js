import React from "react";

const PricePagePreview = ({ entry }) => {
  return (
    <div>
      <h1>Preview Harga</h1>
      {/* Kamu bisa tambahkan preview data dari entry jika mau */}
    </div>
  );
};

export default PricePagePreview;
